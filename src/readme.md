 1. install react project = npm create@vitelates 
 2. create folder structure = src,public,package.json,vite.config.js,
 html and js files

 4. install tailwindcss = npm install -D tailwindcss postcss autoprefixer
 5. create tailwind.config.js = npx tailwindcss init -postcss
 6. add tailwindcss to vite.config.js
 7. start the project = npm run dev
 8. used in useReduser  = manage quiz state in react  useReducer () = question,answer,current question,quiz status.
 9. used in useEffect = manage timer & countdown in react
 10. tailwind css for styling 
 11. ArrayMethods = used in quiz app for managing questions and answers
 12. spread operator = immutable state update (...state)
 13.Conditional rendering → Start screen, Quiz screen, Result screen
 Event handling → answer selection, Next, Previous, Submit